# ShoppyGlobe - backend

## Installation
1. Clone the repository (git clone (https://github.com/koushik2411/ShoppyGlobe-BE/tree/main))
2. Install (npm install)
3. Make a .env file with MONGO_URI, PORT, and JWT_KEY.
4. Launch app (npm start / npm run dev)
